import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    starRatingContainer: {
        flexDirection: "row",
        alignItems: "center",
        gap: 10
    },
    star: {
        fontSize: 20
    }
});